- 默认的shell环境~/.zshrc，python环境使用conda，包管理使用uv
- 未经许可不得执行 git commit/push 操作
- 不要在当前环境下直接运行重的任务，例如大量数据搬运、GPU实验等；都需通过slurm申请对应资源
- GPU实验尽量用Zero2
- 完成代码更改或者功能实现后同步README。
- 目标是根据idea完成一篇简单的CVPR示例投稿：控制主实验不超过3组，数据构造要求不大，正文页数不超过6页，核心改进点小而有效，涵盖基础结论即可。