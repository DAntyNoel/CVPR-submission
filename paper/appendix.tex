\section{Supplementary Material}
\captionsetup{hypcap=false}
\footnotesize

This appendix records the controls behind the same bounded conclusion in the
main paper: reproducibility setup, additional quantitative checks, and
qualitative/scoring details.

\subsection{Reproducibility Setup and Artifacts}

All trained variants use the same backbone, optimizer objective, adapter
configuration, and distributed setup. The only experimental variable is the
composition of preference rows: answer rows, verifier rows, or their mixture.
This control is important because the paper interprets differences between
groups as evidence about supervision format rather than architecture,
optimizer, or decoding changes.

\begin{center}
\begin{minipage}{0.58\linewidth}
\centering
\footnotesize
\begin{tabular}{ll}
\toprule
Setting & Value \\
\midrule
Backbone & Qwen2.5-VL-7B-Instruct \\
Training objective & Direct Preference Optimization \\
Adapter & LoRA \\
Epochs & 1 \\
DPO beta & 0.1 \\
LoRA rank & 16 \\
Answer rows & 0 or 6{,}000 \\
Answer rows in fixed-budget controls & 4{,}000 or 5{,}000 \\
Verifier rows & 0, 500, 1{,}000, or 2{,}000 \\
Distributed setup & ZeRO-2 \\
\bottomrule
\end{tabular}
\captionof{table}{Shared training setup for the preference-tuned groups. The
main paper reports five groups; the 500-verifier-row run is retained only as
an auxiliary sensitivity check.}
\label{tab:app_train}
\end{minipage}
\end{center}

The completed artifacts are small enough to inspect and regenerate. The
canonical \dataset{} file contains 6{,}000 rows. The selected \dualtwok{} DPO
export contains 8{,}000 rows: 6{,}000 answer-preference rows plus 2{,}000
verifier rows. The evidence probes are fixed at 600 supported-evidence rows
and 400 wrong-evidence rows. This fixed-probe design prevents the ablation from
changing both the training data and the evaluation set at the same time.

Lightweight local regeneration covers data checking, audit-sheet completion,
CEPO-Dual export, and LLaMA-Factory data preparation. Heavy training and full
inference are submitted through Slurm, following the repository rule that GPU
jobs should not be run directly in the interactive environment. The relevant
Slurm entry points are:
\begin{quote}
\footnotesize
\texttt{submit\_cepo\_dual\_pipeline.sh}\\
\texttt{submit\_cepo\_ablation\_pipeline.sh}\\
\texttt{submit\_cepo\_fixed\_budget\_pipeline.sh}
\end{quote}
The paper numbers are then refreshed with the deterministic scoring and
summary scripts:
\begin{quote}
\footnotesize
\texttt{score\_object\_eval.py}\\
\texttt{score\_cepo\_evidence\_probe.py}\\
\texttt{bootstrap\_cepo\_probe\_ci.py}\\
\texttt{summarize\_cepo\_ablation\_results.py}\\
\texttt{summarize\_cepo\_fixed\_budget\_results.py}
\end{quote}

\subsection{Additional Quantitative Checks}

The main paper keeps the primary comparison to five groups, as required by the
submission plan. Table~\ref{tab:app_ablation} includes the auxiliary
CEPO-Dual-500 run that was useful during analysis but intentionally omitted
from the main body. The table supports two readings. First, verifier-only
supervision improves wrong-evidence rejection but does not replace answer
preference tuning for ordinary short-answer transfer. Second, the mixed
CEPO-Dual variants need enough verifier data before the wrong-evidence probe
clearly improves: CEPO-Dual-500 and CEPO-Dual-1k are weaker than the selected
CEPO-Dual-2k setting.

\begin{center}
\begin{minipage}{\linewidth}
\centering
\scriptsize
\setlength{\tabcolsep}{3.5pt}
\begin{tabular}{@{}lrrrrrrrr@{}}
\toprule
Setting & Ans. rows & Ver. rows & COCO & GQA & Hard & Supp. & Wrong Rej. & Obj/Attr/Rel \\
\midrule
Base Instruct & 0 & 0 & 96.0 & 76.2 & 94.4 & 86.5 & 44.3 & 59.5 / 53.7 / 17.6 \\
\answerdpo{} & 6{,}000 & 0 & 97.0 & 76.8 & 95.0 & 90.7 & 34.3 & 45.2 / 45.0 / 10.4 \\
\evidencedpo{} & 0 & 2{,}000 & 95.9 & 76.7 & 94.4 & 87.8 & 53.0 & 77.0 / 63.8 / 16.0 \\
CEPO-Dual-500 & 6{,}000 & 500 & 96.9 & 76.8 & 95.0 & 91.3 & 39.5 & 56.3 / 50.3 / 9.6 \\
\dualonek{} & 6{,}000 & 1{,}000 & 97.0 & 77.2 & 94.9 & 93.2 & 48.5 & 74.6 / 59.1 / 9.6 \\
\dualtwok{} & 6{,}000 & 2{,}000 & 97.0 & 77.1 & 94.9 & 95.2 & 70.3 & 96.0 / 85.9 / 25.6 \\
\bottomrule
\end{tabular}
\captionof{table}{Full verifier-count ablation from the released artifacts.
The main paper omits CEPO-Dual-500 to keep the primary comparison to at most
five groups.}
\label{tab:app_ablation}
\end{minipage}
\end{center}

The verifier-count sweep above still changes total training rows. To isolate
this confound, Table~\ref{tab:app_fixed_budget} keeps the total budget at
6{,}000 rows and replaces answer rows with verifier rows. Dual-2k-fixed6k
preserves the short-answer transfer of \answerdpo{}-6k while improving
wrong-evidence rejection by 28.5 points. This supports the main-text claim
that verifier replacement helps beyond simply adding more preference rows.

\begin{center}
\begin{minipage}{\linewidth}
\centering
\scriptsize
\setlength{\tabcolsep}{3.2pt}
\begin{tabular}{@{}lrrrrrr@{}}
\toprule
Setting & Ans. & Ver. & Total & COCO/GQA/Hard & Supp. & Wrong Rej. \\
\midrule
\answerdpo{}-6k & 6{,}000 & 0 & 6{,}000 & 97.0 / 76.8 / 95.0 & 90.7 & 34.3 \\
Answer-4k & 4{,}000 & 0 & 4{,}000 & 96.7 / 77.0 / 94.9 & 89.2 & 39.2 \\
Dual-1k-fixed6k & 5{,}000 & 1{,}000 & 6{,}000 & 96.9 / 77.0 / 95.1 & 91.5 & 48.2 \\
Dual-2k-fixed6k & 4{,}000 & 2{,}000 & 6{,}000 & 96.9 / 76.9 / 95.1 & 94.5 & 62.7 \\
\bottomrule
\end{tabular}
\captionof{table}{Fixed-budget control for the row-count confound. Values are
percentages. The fixed-budget rows are appendix controls only and do not alter
the five-group main comparison.}
\label{tab:app_fixed_budget}
\end{minipage}
\end{center}

Table~\ref{tab:app_seed} summarizes the seed-stability run used to check
LoRA-DPO variance. The default seed-42 rows are the locked V3 runs; seeds 13
and 97 are the completed retry jobs with serialized preprocessing to avoid
shared-cache races. The wrong-evidence gap remains large for all three seeds:
\dualtwok{} improves over \answerdpo{} by 38.8, 36.0, and 35.2 points for
seeds 13, 42, and 97 respectively, while short-answer accuracy stays nearly
unchanged.

\begin{center}
\begin{minipage}{\linewidth}
\centering
\scriptsize
\setlength{\tabcolsep}{3.2pt}
\begin{tabular}{@{}rllrrrr@{}}
\toprule
Seed & Model & COCO/GQA/Hard & BEM & Supp. & Wrong Rej. & Obj/Attr/Rel \\
\midrule
13 & \answerdpo{} & 96.9 / 77.2 / 95.1 & 19.4 & 90.7 & 33.0 & 42.1 / 44.3 / 10.4 \\
13 & \dualtwok{} & 97.0 / 76.9 / 95.0 & 20.3 & 95.7 & 71.8 & 96.0 / 85.9 / 30.4 \\
42 & \answerdpo{} & 97.0 / 76.8 / 95.0 & 18.0 & 90.7 & 34.3 & 45.2 / 45.0 / 10.4 \\
42 & \dualtwok{} & 97.0 / 77.1 / 94.9 & 19.4 & 95.2 & 70.3 & 96.0 / 85.9 / 25.6 \\
97 & \answerdpo{} & 97.0 / 77.0 / 95.1 & 19.0 & 90.2 & 33.3 & 42.9 / 45.0 / 9.6 \\
97 & \dualtwok{} & 97.0 / 76.9 / 95.0 & 19.5 & 95.3 & 68.5 & 96.8 / 85.2 / 20.0 \\
\bottomrule
\end{tabular}
\captionof{table}{Seed-stability results for the two trained groups needed
for the central conclusion. Values are percentages.}
\label{tab:app_seed}
\end{minipage}
\end{center}

The row-preserving paraphrased probe rewrites the evaluation question and
candidate-evidence wording while keeping the locked probe images, rows, and
labels. It gives a weaker but useful template-robustness check: Base,
\answerdpo{}, and \dualtwok{} obtain 65.8\%, 61.8\%, and 73.2\%
wrong-evidence rejection, respectively, with 86.0\%, 90.0\%, and 90.0\%
supported accuracy and 0.0\% parse failure. The detailed slice breakdown is
stored in the released \texttt{paraphrase\_probe/metrics.csv} artifact.

The ablation also shows why the paper does not present CEPO-Dual as a broad
hallucination method. The largest gain is on the internal evidence probe, not
on every external yes/no benchmark. The main paper therefore reports POPE and
AMBER only as sanity checks: CEPO-Dual-2k stays close to CEPO Answer-DPO, so
the verifier rows do not cause an obvious collapse in ordinary polling
behavior, but they are not the source of the main claim.

\begin{center}
\begin{minipage}{0.74\linewidth}
\centering
\footnotesize
\begin{tabular}{lrrrr}
\toprule
Model & Overall & Swap & L/R reversal & False accept \\
\midrule
Base Instruct & 60.8 & 21.7 & 100.0 & 39.2 \\
\answerdpo{} & 55.4 & 10.8 & 100.0 & 44.6 \\
\dualtwok{} & \textbf{71.3} & \textbf{42.5} & 100.0 & \textbf{28.8} \\
\bottomrule
\end{tabular}
\captionof{table}{Locked relation-stress probe. It contains 240 held-out GQA
relation rows split evenly between subject/object swaps and left/right
reversals. The residual weakness is role swapping, not simple left/right
lexical reversal.}
\label{tab:app_relation_stress}
\end{minipage}
\end{center}

\subsection{Scoring Rules and Qualitative Reading}

The most persistent failure mode is relation verification. Object and
attribute evidence can often be checked by recognizing one label or property,
but a relation requires the model to preserve subject, object, and direction
at the same time. The main-paper examples show both outcomes: CEPO-Dual-2k can
reject some swapped evidence, but if both objects are visible, it may still
accept the swapped relation as supported.

Scoring follows the same conservative principle. Short-answer metrics use a
deterministic yes/no parser. Outputs with explicit uncertainty such as
``cannot determine'' or ``not sure'' are refusals; outputs without a parseable
yes/no decision are counted as other/invalid. The evidence-probe scorer first
tries to parse a JSON object from the raw output and then falls back to labeled
fields; decoding itself is not constrained. For wrong-evidence
rows, both \texttt{wrong\_evidence} and \texttt{contradicted} count as
rejecting the candidate evidence, because both labels indicate that the
evidence should not be accepted as support for the claim. This scoring rule is
why the released metrics retain parser-audit fields even though the paper
tables omit the constant parser columns: an evidence-verification gain should
not come from malformed output or fragile parser recovery.
