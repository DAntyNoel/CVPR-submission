% CVPR 2026 Paper Template; see https://github.com/cvpr-org/author-kit

\documentclass[10pt,twocolumn,letterpaper]{article}

%%%%%%%%% PAPER TYPE  - PLEASE UPDATE FOR FINAL VERSION
% \usepackage{cvpr}              % To produce the CAMERA-READY version
\usepackage[review]{cvpr}      % To produce the REVIEW version
% \usepackage[pagenumbers]{cvpr} % To force page numbers, e.g. for an arXiv version

% Import additional packages in the preamble file, before hyperref
\input{preamble}
\input{local_xetex_fonts}
\usepackage{microtype}

\definecolor{cvprblue}{rgb}{0.21,0.49,0.74}
\definecolor{ceposupport}{rgb}{0.10,0.46,0.22}
\definecolor{ceposupportbg}{rgb}{0.90,0.97,0.92}
\definecolor{cepowrong}{rgb}{0.72,0.16,0.16}
\definecolor{cepowrongbg}{rgb}{1.00,0.92,0.90}
\definecolor{cepopanel}{rgb}{0.78,0.82,0.86}
\setlength{\textfloatsep}{8pt plus 2pt minus 2pt}
\setlength{\floatsep}{7pt plus 2pt minus 2pt}
\setlength{\intextsep}{7pt plus 2pt minus 2pt}
\setlength{\abovecaptionskip}{3pt}
\setlength{\belowcaptionskip}{0pt}
\usepackage[pagebackref,breaklinks,colorlinks,allcolors=cvprblue]{hyperref}

%%%%%%%%% PAPER ID  - PLEASE UPDATE
\def\paperID{*****}
\def\confName{CVPR}
\def\confYear{2026}

\newcommand{\benchmark}{CEPO-Probe}
\newcommand{\dataset}{ClaimEvidence-6K}
\newcommand{\cepo}{CEPO}
\newcommand{\answerdpo}{CEPO Answer-DPO}
\newcommand{\evidencedpo}{Evidence-DPO-only}
\newcommand{\dualonek}{CEPO-Dual-1k}
\newcommand{\dualtwok}{CEPO-Dual-2k}
\newcommand{\cepodual}{CEPO-Dual-2k}

%%%%%%%%% TITLE - PLEASE UPDATE
\title{\benchmark{}: Probing Claim-Evidence Consistency in Vision-Language Preference Tuning}

%%%%%%%%% AUTHORS - PLEASE UPDATE
\author{Anonymous \confName{} Submission}

\begin{document}
\maketitle

\begin{abstract}
Preference tuning can make vision-language models better short-answerers
without making them better evidence checkers. We introduce \emph{Claim-Evidence
Preference Optimization} Probe (\benchmark{}), a diagnostic benchmark that
pairs each image question with a candidate claim and a candidate evidence item.
The task is to decide whether the evidence supports the claim, including
same-answer cases where the final answer remains plausible but the evidence is
wrong. In a controlled Qwen2.5-VL-7B DPO study, answer-only tuning improves
ordinary yes/no accuracy but lowers wrong-evidence rejection from 44.3\% to
34.3\%. Direct verifier preferences reverse this failure mode: a verifier-only
control reaches 53.0\%, and the strongest mixed answer/verifier recipe reaches
70.3\% while preserving the short-answer gains. The improvement is uneven:
object and attribute evidence benefit most, while relation role swaps remain
fragile. A same-family 32B QLoRA check serves as a boundary condition:
CEPO-Dual-2k only recovers the answer-only drop back to the 32B base level
(24.5\% vs. 23.8\%), showing that scaling within this family does not
automatically solve evidence rejection. With bootstrap intervals, seed and
fixed-budget controls, paraphrased prompts, parser audits, and slice analysis,
\benchmark{} supports a bounded claim: explicit verifier preferences improve a
measured claim-evidence consistency skill in the controlled 7B setting, but do
not solve visual grounding.
\end{abstract}

\section{Introduction}

Vision-language models can answer visual questions fluently while accepting
the wrong visual support. A model may correctly answer that a sink is present
but accept ``potted plant'' as the evidence, or it may accept a left/right
relation after the subject and object have been swapped. These errors matter
because many preference-tuning pipelines supervise the final answer, while the
connection between the answer and its visual evidence remains implicit.

Direct Preference Optimization (DPO)~\cite{dpo} is attractive for VLM
alignment because it trains from chosen/rejected pairs without learning a
separate reward model. However, an answer-only preference pair can be satisfied
by learning a better response prior or a stronger yes/no bias. If evaluation
also asks only for short answers, a model can appear improved while still
accepting evidence that contradicts the intended visual claim. This raises a
diagnostic question: when preference tuning improves ordinary answers, does it
also improve the ability to verify claim-evidence consistency?

We introduce \emph{Claim-Evidence Preference Optimization Probe}
(\benchmark{}), a controlled diagnostic benchmark designed to isolate this
question. Each example contains an image, a question, a candidate claim, and a
candidate evidence item. The model returns a structured judgment indicating
whether the evidence supports the claim. The key split is wrong-evidence
verification: the answer text may remain plausible, but the candidate evidence
is mismatched. This forces evaluation to look beyond the final answer and ask
whether the model accepts the correct object, attribute, or spatial relation.

We focus on a controlled diagnostic setting. \dataset{} contains 6{,}000
training rows derived from existing COCO~\cite{coco} and GQA~\cite{gqa}
annotations, with a fixed 600-row supported-evidence probe and 400-row
wrong-evidence probe. The main paper limits the training comparison to five
groups: Base Instruct, \answerdpo{}, \evidencedpo{}, \dualonek{}, and
\dualtwok{}. This keeps the experimental surface narrow enough for a
reproducible diagnostic conclusion while still testing whether verifier
supervision helps by itself and when mixed with answer preferences.

Our conclusion is deliberately bounded. We do not claim that \dualtwok{}
solves visual hallucination. Instead, \benchmark{} shows that answer-level DPO
and evidence verification can move independently. \answerdpo{} improves
ordinary short-answer behavior but weakens wrong-evidence rejection. Direct
verifier preferences, especially at 2k rows, improve explicit evidence
consistency without collapsing the short-answer gains. The remaining relation
failures are central to the result: a model can learn to reject many wrong
object and attribute labels while still accepting subject/object swaps.

Our contributions are:
\begin{itemize}
    \item We define \benchmark{}, a claim-evidence consistency benchmark that
    separates answer correctness from evidence correctness.
    \item We construct \dataset{}, a controlled 6k-row preference collection
    with object, attribute, relation, and same-answer wrong-evidence examples,
    plus fixed supported and wrong-evidence probes.
    \item We provide a deterministic protocol that reports short-answer
    transfer, support accuracy, wrong-evidence rejection, slice breakdowns,
    bootstrap intervals, and parser quality.
    \item We show in a five-group comparison that answer-only DPO can weaken
    wrong-evidence rejection, while direct verifier preferences improve the
    explicit probe and reveal relation role swaps as the main bottleneck.
    \item We add fixed-budget, paraphrased-prompt, seed-stability, and
    same-family 32B checks that bound the interpretation, showing both the
    evidence-verifier gain and the limits of scaling and template robustness.
\end{itemize}

\section{Related Work}

\paragraph{Object hallucination and VLM evaluation.}
Object hallucination has been measured by comparing generated captions or
answers with visual annotations. CHAIR~\cite{chair} evaluates hallucinated
objects in captions, while POPE~\cite{pope} turns hallucination detection into
controlled yes/no polling. Broader public evaluations such as
MME~\cite{mme}, MMHal-Bench~\cite{llavarlhf}, AMBER~\cite{amber}, and
HallusionBench~\cite{hallusionbench} report hallucination, perception, and
reasoning failures across response-level or question-answer settings. These
benchmarks are useful for answer-level or caption-level behavior.
\benchmark{} asks a narrower question: given a candidate claim and a
candidate evidence item, does the model accept the evidence as support?

\paragraph{Preference tuning.}
DPO~\cite{dpo} and related preference objectives align models from paired
preferences. Recent multimodal alignment work extends this idea with
factually augmented RLHF~\cite{llavarlhf}, hallucination-aware DPO
preferences~\cite{hadpo}, fine-grained correctional feedback~\cite{rlhfv}, and
preference distillation~\cite{silkie}. In VLM settings, the easiest preference
pair often compares two final answers. Such supervision may improve the answer
distribution without forcing the model to inspect which visual fact supports
the answer. Our experiment keeps the optimizer and backbone fixed while
changing only whether the preference rows supervise answer choice, evidence
verification, or both.

\paragraph{Grounding and rationales.}
Grounded VLM training may use boxes, region descriptions, or
human-written rationales, as in dense region annotation, referring-expression,
and explanation datasets~\cite{visualgenome,flickr30kentities,referitgame,vqax}.
These signals are stronger than the textual evidence labels used here, but
they are also more expensive and test a different capability. \benchmark{} is
not a replacement for region-grounding benchmarks or rationale evaluation: it
asks whether a specified evidence item should be accepted as support under a
preference-tuned policy. This lighter route derives object labels, attributes,
and left/right relations from COCO and GQA annotations, making the benchmark
easy to regenerate and well suited for a controlled diagnostic setting, at the
cost of not measuring precise region localization.

\paragraph{Structured evaluation.}
Many VLM failures are hidden when all outputs are reduced to a final answer.
\benchmark{} therefore requires a structured support decision and audits both
JSON-object parsing and fallback scoring. The purpose is not to reward verbose
explanations, but to expose whether a model accepts or rejects a specific
evidence item under a deterministic scoring rule.

\section{\benchmark{} Benchmark}

\subsection{Task Definition}

Each example contains an image $I$, a visual question $q$, a candidate claim
$c$, and candidate evidence $e$. The model receives the image and a prompt
similar to:
\begin{quote}
\small
\textit{Visual question: Is there a sink in the image? Candidate claim: sink
visible. Candidate evidence: label=potted plant. Decide whether the candidate
evidence supports the claim in the image.}
\end{quote}
The expected output is a JSON object with fields such as
\texttt{answer}, \texttt{claim}, \texttt{evidence\_label}, and
\texttt{support}. The support label is one of \texttt{supported},
\texttt{contradicted}, or \texttt{wrong\_evidence}. We keep the three-way label
in the raw output for auditing, but the main wrong-evidence metric collapses
\texttt{contradicted} and \texttt{wrong\_evidence} into a binary rejection
decision: for the scientific question in this paper, either label means the
candidate evidence should not be accepted as support for the claim.
Figure~\ref{fig:overview} contrasts this probe with ordinary answer-level
evaluation.

\begin{figure}[t]
\centering
\scriptsize
\setlength{\fboxsep}{2pt}
\newcommand{\figstep}[1]{\fbox{\begin{minipage}{0.78\linewidth}\centering #1\end{minipage}}}
\newcommand{\figgood}[1]{\fcolorbox{ceposupport}{ceposupportbg}{\begin{minipage}{0.78\linewidth}\centering\textcolor{ceposupport}{#1}\end{minipage}}}
\newcommand{\figbad}[1]{\fcolorbox{cepowrong}{cepowrongbg}{\begin{minipage}{0.78\linewidth}\centering\textcolor{cepowrong}{#1}\end{minipage}}}
\fcolorbox{cepopanel}{white}{%
\begin{minipage}[t]{0.42\linewidth}
\centering
\textbf{Ordinary yes/no}\par\vspace{2pt}
\figstep{image\\question}\par\vspace{1pt}
$\downarrow$\par\vspace{1pt}
\figstep{answer\\\textcolor{ceposupport}{yes}}\par\vspace{1pt}
$\downarrow$\par\vspace{1pt}
\figstep{answer\\accuracy}\par\vspace{2pt}
{\tiny\textcolor{cepowrong}{evidence untested}}
\end{minipage}}%
\hfill
\fcolorbox{cepopanel}{white}{%
\begin{minipage}[t]{0.42\linewidth}
\centering
\textbf{\benchmark{}}\par\vspace{2pt}
\figstep{image + question\\claim: sink}\par\vspace{1pt}
\figgood{evidence: sink}\par\vspace{1pt}
\figbad{evidence: plant}\par\vspace{1pt}
\figstep{support\\accept/reject}\par\vspace{2pt}
{\tiny\textcolor{cepowrong}{same answer, wrong evidence}}
\end{minipage}}
\caption{\benchmark{} overview. Ordinary yes/no evaluation scores only the
final answer. \benchmark{} scores the claim-evidence link, marking supported
evidence in green and same-answer wrong evidence in red.}
\label{fig:overview}
\end{figure}

\subsection{Data Construction}

\dataset{} is derived from COCO and GQA annotations. COCO contributes
object-existence claims with visible object labels and absent-object negatives.
GQA contributes color/material attributes and left/right spatial relations
from scene graphs. We also create same-answer wrong-evidence rows where the
answer text is intentionally not the discriminating signal: the candidate
evidence is a wrong object label, mismatched attribute, or reversed relation.
Examples include ``person visible'' with \texttt{label=airplane}, ``grass is
green'' with an unrelated object, and a left/right claim whose evidence swaps
subject and object. The answer remains plausible; the support decision should
change.

\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lrr}
\toprule
Construction stratum & Rows & Negative type \\
\midrule
Object existence & 2{,}000 & absent/wrong object \\
Attribute color/material & 1{,}500 & attribute mismatch \\
Left/right relation & 1{,}500 & relation reversal \\
Same-answer wrong evidence & 1{,}000 & evidence mismatch \\
\midrule
Total & 6{,}000 & -- \\
\bottomrule
\end{tabular}
\caption{\dataset{} composition. Rows are construction strata; same-answer
rows are evidence-mismatch cases. Source mix: 2{,}390 COCO-derived and
3{,}610 GQA-derived rows.}
\label{tab:data}
\end{table}

The supported-evidence probe contains 600 rows: 162 object, 276 attribute, and
162 relation examples. The wrong-evidence probe contains 400 rows: 126 object,
149 attribute, and 125 relation examples. The canonical data check reports no
blocking errors, no identical \answerdpo{} chosen/rejected rows, 1{,}000
same-answer wrong-evidence rows, and zero train/eval image overlap against
COCO held-out, GQA simple, Hard COCO, Base-error-mined, POPE, and AMBER evals.

We ran a 200-row annotation-consistency audit before training. All sampled
chosen answers match their supported claims (200/200), all applicable rejected
answers match contradicted claims (170/170), all chosen evidence entries match
the chosen claims (200/200), and all rejected errors match the intended
negative type (200/200). The remaining 30 rejected-answer checks are marked
not applicable because same-answer wrong-evidence rows intentionally keep the
answer plausible while changing only the evidence. The sample is stratified
across 60 object rows, 50 attribute rows, 60 relation rows, and 30
same-answer wrong-evidence rows.

\paragraph{Data card.}
The benchmark is intended for diagnostic evaluation of VLM grounding and
preference tuning. It uses annotation-derived labels from COCO and GQA, while
POPE and AMBER are used only as external evaluation sources. The repository
stores paths and metadata rather than redistributing images. Known limitations
include annotation incompleteness, weak textual evidence labels, missing boxes
for some GQA-derived rows, and narrow coverage of simple object, attribute,
and left/right relation claims.

\section{Preference Tuning Setup}

\subsection{Training Groups}

The main controlled variants use Qwen2.5-VL-7B-Instruct~\cite{qwen25vl} as the
backbone, LoRA~\cite{lora} rank 16, one training epoch, DPO $\beta=0.1$, and a
ZeRO-2~\cite{zero} distributed setup. The comparison changes only the
preference rows. Table \ref{tab:groups} lists the five groups used for the
main ablation. An
auxiliary 500-verifier-row run is kept in the released experiment artifacts,
but it is not part of the main comparison so that the 7B ablation stays within
five primary groups.

We also run a minimal Qwen2.5-VL-32B-Instruct~\cite{qwen25vl32b} check: Base
Instruct, \answerdpo{}, and \dualtwok{} with seed 42. This is not a second
controlled ablation. It uses the same model family and, for resource reasons,
the reported tuned rows use a 4-bit QLoRA~\cite{qlora} path rather than the
full 7B LoRA recipe. We use these rows only to ask whether a larger in-family
backbone automatically fixes evidence rejection or whether the selected mixed
verifier recipe causes short-answer collapse. The 32B training setup and
ZeRO-3 preflight are documented in the appendix.

\begin{table*}[t]
\centering
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{@{}lrrp{0.27\linewidth}p{0.27\linewidth}@{}}
\toprule
Group & Answer rows & Verifier rows & Chosen response & Rejected response \\
\midrule
Base Instruct & 0 & 0 & No preference tuning & No preference tuning \\
\answerdpo{} & 6{,}000 & 0 & Short answer matching the supported claim & Short answer matching the contradicted claim \\
\evidencedpo{} & 0 & 2{,}000 & JSON verifier judgment that accepts supported evidence or rejects wrong evidence & JSON verifier judgment with the opposite support decision \\
\dualonek{} & 6{,}000 & 1{,}000 & Mixed answer and verifier positives & Mixed answer and verifier negatives \\
\dualtwok{} & 6{,}000 & 2{,}000 & Mixed answer and verifier positives & Mixed answer and verifier negatives \\
\bottomrule
\end{tabular}
\caption{Main training groups for the controlled 7B ablation.}
\label{tab:groups}
\end{table*}

\subsection{Verifier Preference Format}

The verifier preference rows use the same image and natural-language context
as the answer rows, but the chosen/rejected responses are structured evidence
judgments. For a supported object example, the chosen response marks
\texttt{support=supported} and repeats the candidate evidence label. For a
wrong-evidence example, the chosen response marks
\texttt{support=wrong\_evidence} or \texttt{support=contradicted}, while the
rejected response incorrectly accepts the mismatched evidence as supported.

\begin{table*}[t]
\centering
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{@{}p{0.17\linewidth}p{0.27\linewidth}p{0.25\linewidth}p{0.23\linewidth}@{}}
\toprule
Preference row & Prompt content & Chosen response & Rejected response \\
\midrule
Answer-DPO &
Image plus question: ``Is there a sink in the image?'' &
Short answer consistent with the supported claim, e.g., ``Yes.'' &
Short answer consistent with the contradicted claim, e.g., ``No.'' \\
Supported verifier &
Image, question, claim ``sink visible'', evidence ``label=sink'' &
JSON with \texttt{support=supported} and \texttt{evidence\_label=sink} &
JSON that rejects the supported evidence \\
Wrong-evidence verifier &
Image, question, claim ``sink visible'', evidence ``label=potted plant'' &
JSON with \texttt{support=wrong\_evidence} or \texttt{contradicted} &
JSON that incorrectly accepts the candidate evidence as supported \\
\bottomrule
\end{tabular}
\caption{Preference formats. Verifier rows keep the visual context but
supervise the support decision directly.}
\label{tab:format}
\end{table*}

This format is intentionally simple. It does not require boxes, masks, or
human-written rationales. It only asks the model to decide whether the
candidate evidence item is a valid support for the claim. The lightweight
format keeps the data construction bounded for a diagnostic submission,
but it also explains a limitation of the results: success on \benchmark{} is
evidence-label consistency, not full visual grounding.

The separation between answer rows and verifier rows is important for
interpretation. Answer-DPO rows can improve the short-answer policy even if the
model never learns to reject an incorrect evidence label. Verifier rows can
teach a support decision, but by themselves they do not necessarily improve the
ordinary yes/no prompt. The mixed groups test whether these two supervision
signals can be combined without turning the study into a large data-scaling
exercise.

\section{Evaluation Protocol}

\subsection{Short-Answer Transfer}

Short-answer transfer uses four fixed yes/no evaluations. COCO object
existence and Hard COCO test object presence under normal and harder negative
sampling. GQA simple tests object, attribute, and relation questions from scene
graphs. Base-error-mined (BEM) recovery contains examples the base model gets
wrong; the base row is therefore 0 by construction, and the metric reports how
often tuning recovers those base errors. We report accuracy plus false-positive
rate (FPR) and false-negative rate (FNR) to expose yes/no bias.

\subsection{Claim-Evidence Metrics}

\benchmark{} reports supported-evidence accuracy on the 600-row supported
probe and wrong-evidence rejection on the 400-row wrong-evidence probe. The
wrong-evidence probe is further broken into object, attribute, and relation
slices. The evidence probe uses ordinary deterministic decoding rather than
constrained decoding; parser quality is audited separately in the released
metrics. The audit reports whether the raw generation contains a parseable JSON
object, whether deterministic fallback scoring was needed, and whether parsing
failed. Because every paper-row evidence-probe output contains a parseable JSON
object and has 0.0\% parse failure, the tables omit these constant columns. For
the primary anchor comparison, we compute row-bootstrap 95\% confidence
intervals with 10{,}000 resamples.

\subsection{Controls and Reporting Discipline}

We include several controls to keep the conclusion narrow. First, all training
and evaluation image identifiers are checked for overlap, so the probe is not
measuring repeated training images. Second, the supported and wrong-evidence
probes are fixed before comparing trained models. Third, the parser audit is
checked separately from the main probe metrics; a method should not receive
credit for a support decision if the scorer recovered only a malformed or
ambiguous output.
Image disjointness does not prove template-distribution independence: training
and probe rows still share annotation sources, prompt style, and evidence
templates. We therefore add an inference-only paraphrased probe for the three
anchor models, keeping the same 600 supported and 400 wrong-evidence rows and
labels while rewriting the question and candidate-evidence wording. On this
row-preserving check, \dualtwok{} still beats \answerdpo{} on wrong-evidence
rejection (73.2\% vs. 61.8\%, +11.5 points) while both reach 90.0\%
supported accuracy and parse failure remains 0.0\%. The absolute paraphrased
numbers should not be compared directly with the original prompt, because the
wording changes the difficulty; we use only the within-probe ordering as a
template-robustness check, not as human-written evidence distribution shift.

The main 7B ablation is also capped at five groups. This is a practical
reporting choice rather than an optimization claim. It keeps the paper focused
on the minimal scientific questions: whether answer-only DPO transfers to
evidence verification, whether verifier-only supervision is sufficient, and
whether mixed answer/verifier supervision improves the explicit probe. The 32B
rows are reported only as same-family capacity checks, while additional
sensitivity runs are kept in the appendix and released artifacts.

\section{Experiments}

\subsection{Short-Answer Transfer}

\begin{table*}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{3.2pt}
\begin{tabular}{@{}lccccccc@{}}
\toprule
Model & COCO Acc & COCO FPR/FNR & GQA Acc & GQA FPR/FNR & Hard Acc & Hard FPR/FNR & BEM Rec. \\
\midrule
Base Instruct & 96.0 & \textbf{1.6} / 6.4 & 76.2 & \textbf{14.4} / 33.2 & 94.4 & \textbf{3.8} / 7.4 & 0.0 \\
\answerdpo{} & \textbf{97.0} & 1.8 / \textbf{4.2} & 76.8 & 23.6 / 22.8 & \textbf{95.0} & 5.0 / \textbf{5.0} & 18.0 \\
\evidencedpo{} & 95.9 & 1.8 / 6.4 & 76.7 & 14.2 / 32.4 & 94.4 & \textbf{3.8} / 7.4 & 1.4 \\
\dualonek{} & \textbf{97.0} & 1.8 / \textbf{4.2} & \textbf{77.2} & 24.2 / \textbf{21.4} & 94.9 & 5.2 / \textbf{5.0} & \textbf{19.8} \\
\dualtwok{} & \textbf{97.0} & 1.8 / \textbf{4.2} & 77.1 & 24.4 / \textbf{21.4} & 94.9 & 5.2 / \textbf{5.0} & 19.4 \\
\midrule
32B Base & 95.7 & 1.4 / 7.2 & 76.6 & 15.4 / 31.4 & 93.1 & 4.4 / 9.4 & 22.6 \\
32B \answerdpo{} & 95.9 & 1.6 / 6.6 & 76.7 & 18.4 / 28.2 & 93.8 & 4.6 / 7.8 & 24.3 \\
32B \dualtwok{} & 95.9 & 1.6 / 6.6 & 76.6 & 19.4 / 27.4 & 93.8 & 4.6 / 7.8 & 24.7 \\
\bottomrule
\end{tabular}
\caption{Short-answer transfer under the normal yes/no prompt. Values are
percentages; BEM Rec. is recovery on the Base-error-mined set.}
\label{tab:short}
\end{table*}

Table~\ref{tab:short} shows that the answer-trained models preserve ordinary
yes/no ability. \answerdpo{} improves COCO accuracy from 96.0\% to 97.0\% and
Hard COCO from 94.4\% to 95.0\%, mainly by lowering FNR. The mixed
\dualonek{} and \dualtwok{} variants keep this recall gain and recover roughly
19\% of BEM examples. The trade-off is visible in FPR: both mixed variants
raise GQA FPR to about 24\%, compared with 14.4\% for the base model.

\evidencedpo{} behaves differently. It receives no answer preference rows, so
it does not improve COCO or Hard COCO and recovers almost none of the BEM
errors. This is useful diagnostically: verifier-only supervision can teach the
probe behavior discussed next, but it is not a replacement for answer
preference tuning when the deployment prompt asks for a short answer.

The 32B rows are a limited in-family sanity check rather than transfer
evidence across model families. They show that adding the verifier rows does
not visibly damage the larger model's short-answer behavior: relative to 32B
\answerdpo{}, COCO and Hard COCO are tied, GQA changes by -0.1 points, and BEM
recovery changes from 24.3\% to 24.7\%. This lets the evidence-probe result
below be read as a weak scaling diagnostic instead of a short-answer trade-off.

\subsection{Claim-Evidence Consistency}

\begin{table*}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{@{}lrrrrr@{}}
\toprule
Model & Supp. Acc & Wrong Rej. & Obj Wrong & Attr Wrong & Rel. Wrong \\
\midrule
Base Instruct & 86.5 & 44.3 & 59.5 & 53.7 & 17.6 \\
\answerdpo{} & 90.7 & 34.3 & 45.2 & 45.0 & 10.4 \\
\evidencedpo{} & 87.8 & 53.0 & 77.0 & 63.8 & 16.0 \\
\dualonek{} & 93.2 & 48.5 & 74.6 & 59.1 & 9.6 \\
\dualtwok{} & \textbf{95.2} & \textbf{70.3} & \textbf{96.0} & \textbf{85.9} & \textbf{25.6} \\
\midrule
32B Base & 91.8 & 24.5 & 39.7 & 22.1 & 12.0 \\
32B \answerdpo{} & 93.5 & 23.8 & 41.3 & 21.5 & 8.8 \\
32B \dualtwok{} & 93.0 & 24.5 & 39.7 & 23.5 & 10.4 \\
\bottomrule
\end{tabular}
\caption{\benchmark{} probe results. Supp. Acc. uses 600 supported-evidence
rows; Wrong Rej. uses 400 wrong-evidence rows, with slice rejection by task
type. Values are percentages.}
\label{tab:probe}
\end{table*}

Table~\ref{tab:probe} is the central diagnostic result. \answerdpo{} improves
supported-evidence accuracy from 86.5\% to 90.7\%, but wrong-evidence rejection
falls from 44.3\% to 34.3\%. The model is better at accepting supported claims
and worse at rejecting plausible but mismatched evidence. This confirms that
answer-level gains cannot be treated as evidence-level gains.

Verifier supervision changes the picture. \evidencedpo{} raises
wrong-evidence rejection to 53.0\%, especially on object and attribute
mismatches, but it does not improve short-answer transfer. \dualonek{} combines
answer preferences with 1k verifier rows and improves supported-evidence
accuracy to 93.2\%, but wrong-evidence rejection remains only 48.5\%.
\dualtwok{} is the only main setting that clearly improves both sides of the
probe: supported-evidence accuracy reaches 95.2\% and wrong-evidence rejection
reaches 70.3\%.

The 32B rows are more cautionary than supportive. The 32B base and answer-only
models already behave differently from the 7B anchors, with higher
supported-evidence accuracy but lower wrong-evidence rejection. One possible
interpretation is that the larger instruction-tuned model is more willing to
rationalize a provided candidate evidence item once the relevant objects are
plausible, which raises supported-evidence accuracy but depresses rejection of
mismatched evidence. \dualtwok{} recovers the small 32B answer-only drop in
wrong-evidence rejection (23.8\% to 24.5\%) and raises the relation
wrong-evidence slice from 8.8\% to 10.4\% while preserving the short-answer
metrics in Table~\ref{tab:short}. However, 24.5\% only matches the 32B base.
This is evidence that the mixed recipe is not obviously harmful at 32B, but it
is also a negative scaling signal: within this Qwen2.5-VL family, larger
capacity does not automatically solve wrong-evidence rejection.

\begin{table}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{2.5pt}
\begin{tabular}{@{}lcc@{}}
\toprule
Model & Supp. 95\% CI & Wrong 95\% CI \\
\midrule
Base Instruct & 86.5 [83.7, 89.2] & 44.3 [39.5, 49.0] \\
\answerdpo{} & 90.7 [88.3, 93.0] & 34.3 [29.8, 39.0] \\
\dualtwok{} & 95.2 [93.3, 96.8] & 70.3 [65.8, 74.8] \\
\bottomrule
\end{tabular}
\caption{Row-bootstrap intervals for the primary anchors using 10{,}000 row
resamples.}
\label{tab:ci}
\end{table}

The intervals in Table~\ref{tab:ci} support the same reading. The
\dualtwok{} wrong-evidence interval [65.8, 74.8] is well separated from both
Base Instruct [39.5, 49.0] and \answerdpo{} [29.8, 39.0]. These are
fixed-output row-bootstrap intervals, not training-run variance. A three-seed
LoRA-DPO check gives the missing stability test: \dualtwok{} improves
wrong-evidence rejection over \answerdpo{} by +35.2 to +38.8 points, averaging
70.2$\pm$1.3 versus 33.5$\pm$0.5, while COCO, GQA, and Hard COCO accuracy stay
within 0.3 points. Parser quality is not a confound: decoding is unconstrained
but the prompt asks for JSON, every anchor probe output contains a parseable
JSON object, fallback scoring was never needed, and parse failure is 0\%.

\subsection{Slice and Error Analysis}

The slice columns in Table~\ref{tab:probe} show that \dualtwok{} does not
improve all evidence types equally. Object wrong-evidence rejection rises to
96.0\% and attribute wrong-evidence rejection rises to 85.9\%. Relation
wrong-evidence rejection reaches only 25.6\%, even though this is higher than
the base and answer-only rows. Row-bootstrap slice intervals for the three
anchor models are: object 96.0 [92.1, 99.2], attribute 85.9 [79.9, 91.3], and
relation 25.6 [18.4, 32.8] for \dualtwok{}. The relation interval remains low,
so the failure is not a small-sample artifact.

\begin{table}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}lrrrr@{}}
\toprule
Model & Overall & Swap & L/R rev. & False acc. \\
\midrule
Base & 60.8 & 21.7 & 100.0 & 39.2 \\
\answerdpo{} & 55.4 & 10.8 & 100.0 & 44.6 \\
\dualtwok{} & \textbf{71.3} & \textbf{42.5} & 100.0 & \textbf{28.8} \\
\bottomrule
\end{tabular}
\caption{Locked relation-stress probe on 240 held-out GQA relation rows.
Values are percentages; higher is better except False acc.}
\label{tab:relation_stress}
\end{table}

The locked stress probe in Table~\ref{tab:relation_stress} clarifies the
failure. All three anchor models reject the simple left/right reversal rows,
so the residual problem is not the lexical direction token alone. The hard
case is role preservation: \dualtwok{} raises subject/object swap rejection
from 10.8\% for \answerdpo{} to 42.5\%, but most swapped-role cases are still
accepted. Representative examples have the right objects and relation word but
swapped roles, such as a claim ``tree right\_of bus'' paired with evidence
``subject=bus; object=tree; relation=right\_of''. A stronger relation probe
likely needs region-aware evidence, explicit object localization, or harder
contrastive relation pairs.

\subsection{External Sanity Checks}

\begin{table}[t]
\centering
\footnotesize
\setlength{\tabcolsep}{2.5pt}
\begin{tabular}{lccc}
\toprule
Eval & Base & Ans.-DPO & Dual-2k \\
\midrule
POPE random & 88.7 & 89.6 & 89.8 \\
POPE popular & 87.7 & 88.5 & 88.7 \\
POPE adversarial & 86.7 & 87.1 & 87.3 \\
AMBER discr. & 87.6 & 88.2 & 88.2 \\
\bottomrule
\end{tabular}
\caption{External yes/no sanity checks under the same deterministic parser.}
\label{tab:external}
\end{table}

Table~\ref{tab:external} checks whether the evidence-aware training causes an
obvious collapse on external yes/no hallucination tests. It does not:
\dualtwok{} stays close to \answerdpo{} on POPE and AMBER. The differences are
small and unclaimed; the table is a negative control showing that the large
\benchmark{} gain is not accompanied by a large loss on ordinary external
yes/no checks. The evidence probe is not meant to predict POPE or AMBER
accuracy, because it measures a different claim-evidence consistency behavior.

\section{Discussion}

\paragraph{Answer and evidence behaviors separate.}
The most important empirical pattern is the disagreement between Tables
\ref{tab:short} and \ref{tab:probe}. In our controlled Qwen2.5-VL setting,
\answerdpo{} improves ordinary answers while lowering wrong-evidence rejection.
\evidencedpo{} improves
wrong-evidence rejection while doing little for short-answer transfer.
\dualtwok{} combines both signals and gives the strongest evidence-probe
result. These behaviors would be hard to see from answer accuracy alone.

\paragraph{Verifier data scale matters.}
\dualonek{} improves supported-evidence accuracy but does not strongly reject
wrong evidence; \dualtwok{} is the first mixed recipe that improves both probe
sides. Its verifier rows are balanced as 1{,}000 supported and 1{,}000 wrong,
covering about 808 object, 607 attribute, and 585 relation examples. However,
CEPO-Dual-500, -1k, and -2k use 6.5k, 7k, and 8k total rows, so the 1k to 2k
jump shows that verifier volume and coverage matter, not a clean
mixture-ratio proof. A fixed-budget control replaces answer rows with verifier
rows at 6k total rows: Dual-2k-fixed6k reaches 62.7\% wrong-evidence rejection
versus 34.3\% for \answerdpo{}-6k, while COCO/GQA/Hard accuracy changes by
-0.1/+0.1/+0.1 points. Thus the 2k verifier gain is not only a total-row
artifact, although the original 8k \dualtwok{} run still combines verifier
supervision with extra rows.

\paragraph{Backbone scale matters.}
The 32B check is a capacity sanity check rather than a second full ablation.
It does not replicate the large 7B margin, and \dualtwok{} only recovers the
32B answer-only wrong-evidence drop back to the 32B base level. Because the run
is same-family, single-seed, and uses QLoRA for resource reasons, it should not
be read as a clean capacity comparison. Its value is narrower and more
cautionary: the answer/verifier separation remains measurable under a larger
Qwen2.5-VL backbone, but scaling within this family does not by itself produce
robust evidence rejection. Cross-family base inference and, ideally,
cross-family tuning would be needed for a model-family generalization claim.

\paragraph{Relation evidence is the bottleneck.}
Object and attribute mismatches are often visible from a single label or
attribute. Relation mismatches require comparing two entities and preserving
their roles. The current textual evidence representation does not require the
model to identify regions, so it can accept a subject/object swap if the two
objects are both present. This is why we treat role-swap errors as a remaining
failure rather than a minor detail.

\paragraph{What the benchmark should and should not support.}
\benchmark{} supports a narrow scientific claim: evidence-verifier
preferences can improve a separately measured claim-evidence consistency
skill, and answer-only DPO can weaken that skill. It should not be used to
claim solved hallucination, robust localization, or general reasoning gains.
The fixed-budget control addresses the row-count confound, the paraphrased
probe addresses template wording, the seed check addresses LoRA-DPO variance
for the central comparison, and the external checks and relation failures keep
the conclusion calibrated.

\paragraph{Implications for data construction.}
The result argues for small but targeted evidence data rather than simply more
answer pairs. The 2k verifier rows are enough to expose a clear gain on object
and attribute wrong-evidence rejection, but they are not enough for reliable
relation verification. A follow-up data design should therefore spend new
annotation budget on harder relation contrasts and region-aware evidence
instead of increasing the same object-label template at larger scale.

\section{Limitations}

\benchmark{} is controlled and annotation-derived. It uses COCO and GQA
labels rather than newly collected human evidence labels. Textual evidence
labels are weaker than boxes, masks, or human rationales, and the rule-based
scorer cannot recognize every semantically valid free-form description. Some
GQA-derived rows lack boxes, so the current probe cannot verify region
localization. The main controlled ablation uses one 7B backbone, one LoRA-DPO
recipe, and one deterministic decoding setup. We add a three-seed check for
the central answer-versus-verifier comparison, a fixed-budget row-count control,
a row-preserving paraphrased probe, and a limited 32B transfer check. The 32B
result is useful as a larger-capacity sanity check, but it is still a
single-seed QLoRA transfer over only Base, \answerdpo{}, and \dualtwok{};
it does not establish cross-family portability. We also do not include an
independently human-written evidence mini-probe, so the paraphrased probe
should be read as a wording check rather than a real distribution-shift test.
Broader model families, human-written evidence, independent paraphrases, and
stronger verifier data would be needed for a general alignment claim. Finally,
the relation results show that accepting or rejecting textual relation evidence
is still far from a robust spatial grounding test.

\section{Conclusion}

We presented \benchmark{}, a diagnostic benchmark for claim-evidence
consistency in VLM preference tuning. The benchmark separates ordinary
short-answer performance from the ability to verify whether a visual claim is
supported by the correct evidence. In a controlled five-group 7B experiment,
\answerdpo{} improves short-answer behavior while weakening wrong-evidence
rejection, \evidencedpo{} improves verifier behavior without improving
short-answer transfer, and \dualtwok{} preserves answer gains while improving
explicit evidence consistency to 70.3\% wrong-evidence rejection. A same-family
32B QLoRA check recovers the small answer-only drop without hurting
short-answer accuracy, but also shows that larger capacity does not
automatically fix evidence rejection. The remaining relation failures are
central to the conclusion: short-answer gains alone can hide persistent
wrong-evidence errors, so evidence consistency should be measured directly.

{
    \footnotesize
    \setlength{\bibsep}{1pt plus 0.1pt}
    \bibliographystyle{ieeenat_fullname}
    \bibliography{references}
}

% Optional supplementary material. Compile `main_full.tex` or run `make full`
% to include the appendix without changing the main review PDF.
\ifdefined\cvprincludeappendix
  \clearpage
  \onecolumn
  \appendix
  \input{appendix}
\fi

\end{document}
